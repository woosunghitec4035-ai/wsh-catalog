# 줄바꿈 정규화 — PC/에디터 달라도 저장소는 항상 LF로 통일
# (Windows 여러 대에서 협업해도 "파일 전체가 바뀐 것처럼 보이는" 충돌 방지)
* text=auto eol=lf

# 바이너리 — git이 줄바꿈 건드리지 않도록 명시(손상 방지)
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
*.pdf  binary
*.mp4  binary
*.woff  binary
*.woff2 binary
*.ttf  binary
*.eot  binary
